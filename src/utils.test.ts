/*import { makeFakeDate } from './mocks';
import { getFormatDate } from './utils';

describe('Function: getFormatDate', () => {
  it('should return date in format DD MMMM', ()=> {
    const mockDate = makeFakeDate();
    const result = getFormatDate(mockDate);
    expect(result).
  });
});*/
